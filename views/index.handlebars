<h1>burgers!</h1>

<h2>burgers that are not devored!</h2>

<ul>
  {{#each burgers}}
    {{#unless devored}}
      {{> burgers/burger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>burgers that are devored!</h2>

<ul>
  {{#each burgers}}
    {{#if devored}}
      {{> burgers/burger-block devored=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devored">devored?</label><br>
    <input type="radio" name="devored" value="1" checked> devored!<br>
    <input type="radio" name="devored" value="0"> Awake
  </div>

  <button type="submit">Add burger</button>
</form>
